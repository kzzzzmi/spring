package com.spring.di;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.ArrayList;
import java.util.List;

public class CentralController {
	private List<RemoteControl> controls;
	private BufferedReader br;
	
	public CentralController() {
		controls = new ArrayList<>();
		br = new BufferedReader(new InputStreamReader(System.in));
	}
	
	public void addController() throws IOException {
		System.out.println("회사명을 입력해주세요 : ");
		String company = br.readLine();
		System.out.println("리모컨의 종류를 입력해주세요 : ");
		int remote = Integer.parseInt(br.readLine());
		
		if(remote == 1) {
			
		} else if(remote == 2) {
			
		} else {
			System.out.println("지원하지 않는 리모컨입니다.");
		}
	}
}
